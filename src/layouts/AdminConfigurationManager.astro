---
import type { ClubsPropsAdminPages } from '../types'
import AdminConfigurationManager from './AdminConfigurationManager.svelte'

const { clubs } = Astro.props as {
	clubs: ClubsPropsAdminPages['clubs']
}
---

<AdminConfigurationManager client:only="svelte" {clubs} />
