---
import type { ClubsPropsAdminPages } from '../types'
import AdminConfigurationManager_ from './AdminConfigurationManager.svelte'

interface Props {
	clubs: ClubsPropsAdminPages['clubs']
}

const { clubs } = Astro.props
---

<AdminConfigurationManager_
	client:only="svelte"
	encodedClubsConfiguration={clubs.encodedClubsConfiguration}
/>
