---
import Aside_ from './Aside.vue'

interface Props {
	showConnectButton?: boolean
}

const { showConnectButton } = Astro.props
---

<Aside_ showConnectButton={showConnectButton} client:only="vue">
	<slot name="aside:connect-button" slot="connect-button" />

	<slot />
</Aside_>
