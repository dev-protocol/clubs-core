---
import { Connect, type ConnectProps } from '../components/Connect'

const { PUBLIC_WALLET_CONNECT_PROJECT_ID: projectId } = import.meta.env

const props = Astro.props as Omit<ConnectProps, 'projectId'>
---

<Connect client:load projectId={projectId} {...props} />
